
general {
    colors = true
    interval = 1
}

battery all {
        format = "%status %percentage %remaining"
}

tztime local {
    format = "%Y-%m-%d %a %r"
}

tztime utc {
    format = "%a %r UTC"
    timezone = "Etc/UTC"
}

tztime cet {
    format = "%a %r CET"
    timezone = "CET"
}

tztime ast {
    format = "%a %r AST"
    timezone = "America/Curacao"
}

order += "battery all"
order += "tztime ast"
order += "tztime utc"
order += "tztime cet"
order += "tztime local"


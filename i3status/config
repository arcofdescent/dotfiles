
general {
    colors = true
    interval = 1
}

order += "battery all"
order += "tztime utc"
order += "tztime myt"
order += "tztime local"

battery all {
        format = "%status %percentage %remaining"
}

tztime local {
    format = "%Y-%m-%d %a %r"
}

tztime utc {
    format = "%H:%M:%S UTC"
    timezone = "Etc/UTC"
}

tztime myt {
    format = "%H:%M:%S MYT"
    timezone = "Asia/Kuching"
}

